using UnityEngine;
using UnityEngine.UI;

static class PickupsUI
{
    static Text gold, orb;
    public static void Update()
    {
        return;
        //if(gold == null || orb == null)
        //{
        //    gold = GameObject.Find("UI/Stats/GoldText").GetComponent<Text>();
        //    orb = GameObject.Find("UI/Stats/OrbText").GetComponent<Text>();
        //}
        //gold.text = Player.instance.gold.ToString();
        //orb.text = Player.instance.orbs.ToString();
    }
}